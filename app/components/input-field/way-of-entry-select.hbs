{{#if @label}}
  <label for="way-of-entry-select" class="block text-sm font-medium text-gray-700">
    {{@label}}
  </label>
{{/if}}
<div ...attributes>
  <PowerSelect
    @placeholder={{this.placeholder}}
    @searchPlaceholder="Typ om te zoeken"
    @options={{this.sortedOptions}}
    @selected={{@value}}
    @searchEnabled={{true}}
    @searchField="name"
    @onChange={{@onSelectionChange}}
    @noMatchesMessage="Geen resultaten"
    @allowClear={{true}}
    @disabled={{@disabled}}
    as |option select|
  >
    {{highlight-substr option.label select.lastSearchedText}}
  </PowerSelect>
</div>