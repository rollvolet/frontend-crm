{{#if @label}}
  <label for="telephone-type-select" class="block text-sm font-medium text-gray-700">
    {{@label}}
  </label>
{{/if}}
<div class="mt-1" ...attributes>
  <PowerSelect
    @options={{this.options}}
    @selected={{@value}}
    @searchEnabled={{false}}
    @onChange={{@onSelectionChange}}
    @allowClear={{false}} as |option select|
  >
    {{option.code}}
  </PowerSelect>
</div>
