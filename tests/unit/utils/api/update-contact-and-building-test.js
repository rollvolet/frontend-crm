import apiUpdateContactAndBuilding from 'rollvolet-crm/utils/api/update-contact-and-building';
import { module, test } from 'qunit';

module('Unit | Utility | api/update-contact-and-building', function () {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = apiUpdateContactAndBuilding();
    assert.ok(result);
  });
});
