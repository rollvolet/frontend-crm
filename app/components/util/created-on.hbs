{{#if (or @date this.by)}}
  {{#if @resource}}
    {{@resource}} created
  {{else}}
    Created
  {{/if}}
  {{#if @date}}
    on {{moment-format @date}}
  {{/if}}
  {{#if this.by}}
    by {{this.by}}
  {{/if}}
{{/if}}