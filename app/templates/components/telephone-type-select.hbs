{{#power-select placeholder=placeholder searchPlaceholder="Typ om te zoeken" options=options selected=selected
     searchEnabled=false onchange=onSelectionChange allowClear=true as |option select|}}
  {{option.name}}
{{/power-select}}

{{md-error-messages errors=errors}}
