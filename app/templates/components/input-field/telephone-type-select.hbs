<PowerSelect @placeholder={{this.placeholder}} @searchPlaceholder="Typ om te zoeken" @options={{this.options}} @selected={{@selected}} @searchEnabled={{false}} @onchange={{this.onSelectionChange}} @allowClear={{true}} as |option|>
  {{option.name}}
</PowerSelect>

<MdErrorMessages @errors={{this.errors}} />
