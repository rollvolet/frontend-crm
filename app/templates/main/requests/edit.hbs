{{#paper-tabs selected="main.requests.edit" borderBottom=true as |tabs|}}
  {{#tabs.tab value="main.requests.edit" href=(href-to "main.requests.edit" model)}}
    Aanvraag
  {{/tabs.tab}}
{{/paper-tabs}}

{{request-panel model=model editMode=editMode
    onOpenEdit=(action (mut editMode) true)
    onCloseEdit=(action (mut editMode) false)}}

{{liquid-outlet}}
