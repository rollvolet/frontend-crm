<PowerSelect @placeholder={{placeholder}} @searchPlaceholder="Typ om te zoeken" @options={{options}} @selected={{selected}} @searchEnabled={{false}} @onchange={{onSelectionChange}} @allowClear={{true}} as |option select|>
  {{option.name}}
</PowerSelect>

<MdErrorMessages @errors={{errors}} />
