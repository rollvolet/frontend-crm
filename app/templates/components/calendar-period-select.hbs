{{#power-select placeholder=placeholder searchPlaceholder="Typ om te zoeken" options=options selected=selectedOption
     searchField="name" onchange=(action "changeSelection")
     noMatchesMessage="Geen resultaten" allowClear=true as |option select|}}
  {{highlight-substr option.name select.lastSearchedText}}
{{/power-select}}

{{md-error-messages errors=errors}}
