{{#if (or @date this.by)}}
  {{#if @resource}}
    {{@resource}} bewerkt
  {{else}}
    Bewerkt
  {{/if}}
  {{#if @date}}
    op {{moment-format @date}}
  {{/if}}
  {{#if this.by}}
    door {{this.by}}
  {{/if}}
{{/if}}
