<PaperTabs @selected="main.requests.edit" @borderBottom={{true}} as |tabs|>
  <tabs.tab @value="main.requests.edit" @href={{href-to "main.requests.edit" this.model}}>
    Aanvraag
  </tabs.tab>
</PaperTabs>

<RequestPanel @model={{model}} @editMode={{this.editMode}} @onOpenEdit={{fn (mut this.editMode) true}} @onCloseEdit={{fn (mut this.editMode) false}} />

<LiquidOutlet />
