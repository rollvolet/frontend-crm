<PowerSelect @placeholder={{this.placeholder}} @searchPlaceholder="Typ om te zoeken" @options={{this.options}} @selected={{this.selectedOption}} @searchField="name" @onchange={{fn this.changeSelection}} @noMatchesMessage="Geen resultaten" @allowClear={{true}} as |option select|>
  {{highlight-substr option.name select.lastSearchedText}}
</PowerSelect>

<MdErrorMessages @errors={{this.errors}} />
