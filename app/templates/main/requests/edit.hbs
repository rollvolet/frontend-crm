<PaperTabs @selected="main.requests.edit" @borderBottom={{true}} as |tabs|>
  <tabs.tab @value="main.requests.edit" @href={{href-to "main.requests.edit" this.model}}>
    Aanvraag
  </tabs.tab>
</PaperTabs>

<RequestPanel @model={{model}} @editMode={{this.editMode}} @onOpenEdit={{action (mut this.editMode) true}} @onCloseEdit={{action (mut this.editMode) false}} />

{{liquid-outlet}}
