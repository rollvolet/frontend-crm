<PowerSelect @placeholder={{this.placeholder}} @searchPlaceholder="Typ om te zoeken" @options={{this.options}} @selected={{selected}} @searchField="firstName" @onchange={{this.onSelectionChange}} @noMatchesMessage="Geen resultaten" @allowClear={{true}} as |option select|>
  {{highlight-substr option.firstName select.lastSearchedText}}
</PowerSelect>

<MdErrorMessages @errors={{this.errors}} />
