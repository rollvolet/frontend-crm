import applyFilterParams from 'rollvolet-crm/utils/apply-filter-params';
import { module, test } from 'qunit';

module('Unit | Utility | apply-filter-params', function () {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = applyFilterParams();
    assert.ok(result);
  });
});
