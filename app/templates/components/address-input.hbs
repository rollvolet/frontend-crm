<PaperInput @textarea={{true}} @class={{this.size}} @label={{this.label}} @value={{this.value}} @errors={{this.errors}} @passThru={{hash rows=3 maxRows=3}} @onChange={{action (mut this.value)}} @onBlur={{this.onBlur}} />
