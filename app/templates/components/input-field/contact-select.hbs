<PowerSelect @placeholder={{this.label}} @searchPlaceholder="Typ om te zoeken" @options={{this.options}} @selected={{@selected}} @searchField="searchName" @onchange={{this.onSelectionChange}} @noMatchesMessage="Geen resultaten" @allowClear={{true}} as |option select|>
  {{highlight-substr option.searchName select.lastSearchedText}}
</PowerSelect>
