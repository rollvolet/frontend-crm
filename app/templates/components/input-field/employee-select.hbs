<PowerSelect @placeholder={{placeholder}} @searchPlaceholder="Typ om te zoeken" @options={{options}} @selected={{selected}} @searchField="firstName" @onchange={{onSelectionChange}} @noMatchesMessage="Geen resultaten" @allowClear={{true}} as |option select|>
  {{highlight-substr option.firstName select.lastSearchedText}}
</PowerSelect>

<MdErrorMessages @errors={{errors}} />
