{{#if @label}}
  <label for="customer-keyword-select" class="block text-sm font-medium text-gray-700">
    {{@label}}
  </label>
{{/if}}
<div ...attributes>
  {{#if @multiple}}
    <PowerSelectMultiple
      @placeholder={{this.placeholder}}
      @searchPlaceholder="Typ om te zoeken"
      @options={{this.options.value}}
      @selected={{@value}}
      @searchEnabled={{true}}
      @searchField="label"
      @onChange={{@onSelectionChange}}
      @noMatchesMessage="Geen resultaten"
      @allowClear={{this.allowClear}}
      @disabled={{or @disabled this.options.isPending}}
      as |option select|>
      {{highlight-substr option.label select.lastSearchedText}}
    </PowerSelectMultiple>
  {{else}}
    <PowerSelect
      @placeholder={{this.placeholder}}
      @searchPlaceholder="Typ om te zoeken"
      @options={{this.options.value}}
      @selected={{@value}}
      @searchEnabled={{true}}
      @searchField="label"
      @onChange={{@onSelectionChange}}
      @noMatchesMessage="Geen resultaten"
      @allowClear={{this.allowClear}}
      @disabled={{or @disabled this.options.isPending}}
      as |option select|>
      {{highlight-substr option.label select.lastSearchedText}}
    </PowerSelect>
  {{/if}}
</div>