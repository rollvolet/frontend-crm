import objectPromiseProxy from 'rollvolet-crm/utils/object-promise-proxy';
import { module, test } from 'qunit';

module('Unit | Utility | object-promise-proxy', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = objectPromiseProxy();
    assert.ok(result);
  });
});
