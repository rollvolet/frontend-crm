<PowerSelect @placeholder={{this.placeholder}} @searchPlaceholder="Typ om te zoeken" @options={{this.options}} @selected={{this.selected}} @searchEnabled={{false}} @onChange={{@onSelectionChange}} @allowClear={{true}} as |option|>
  {{option.name}}
</PowerSelect>

<MdErrorMessages @errors={{@errors}} />
