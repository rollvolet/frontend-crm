import getDocumentLanguageCode from 'rollvolet-crm/utils/get-document-language-code';
import { module, test } from 'qunit';

module('Unit | Utility | getDocumentLanguageCode', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = getDocumentLanguageCode();
    assert.ok(result);
  });
});
