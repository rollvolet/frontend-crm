import deformatDecimalInput from 'rollvolet-crm/utils/deformat-decimal-input';
import { module, test } from 'qunit';

module('Unit | Utility | deformat-decimal-input', function (/*hooks*/) {
  // Replace this with your real tests.
  test('it works', function (assert) {
    let result = deformatDecimalInput();
    assert.ok(result);
  });
});
