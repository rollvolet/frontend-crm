<div class="layout-row">
  <PaperInput @label="Datum" @value={{model.paymentDateStr}} @type="date" @required="style" @errors={{v-get model "paymentDate" "messages"}} @onChange={{action (mut model.paymentDateStr)}} @onBlur={{perform save}} @class="default" />
  <PaperInput @label="Bedrag (€)" @value={{amountInput}} @required="style" @errors={{v-get model "amount" "messages"}} @onChange={{action (mut amountInput)}} @onBlur={{queue (action "formatDecimal" "amount") (perform save)}} @class="small" />
  <InputField::PaymentSelect @value={{model.payment}} @onSelectionChange={{queue (action (mut model.payment)) (perform save)}} @class="md-input-wrapper large u-rel-top-small" />
</div>
{{yield}}