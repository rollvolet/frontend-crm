<PaperButton
  @onClick={{@onClick}}
  @iconButton={{@iconButton}}
  @raised={{not @iconButton}}
  @primary={{not @iconButton}}>
  {{#if @iconButton}}
    {{paper-icon "restore"}}
    <PaperTooltip>Recycleer BTW-attest</PaperTooltip>
  {{else}}
    Recycleer BTW-attest
  {{/if}}
</PaperButton>
