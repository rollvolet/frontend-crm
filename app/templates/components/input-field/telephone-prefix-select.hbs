<PowerSelect @placeholder={{this.placeholder}} @searchPlaceholder="Zoeken" @options={{this.options}} @selected={{@selected}} @searchField="telephonePrefix" @onchange={{this.onSelectionChange}} @noMatchesMessage="Geen resultaten" @allowClear={{true}} as |option select|>
  {{highlight-substr option.telephonePrefix select.lastSearchedText}}
</PowerSelect>

<MdErrorMessages @errors={{this.errors}} />
