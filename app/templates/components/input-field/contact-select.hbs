<PowerSelect @placeholder={{@label}} @searchPlaceholder="Typ om te zoeken" @options={{this.options}} @selected={{this.selected}} @searchEnabled={{true}} @searchField="searchName" @onChange={{@onSelectionChange}} @noMatchesMessage="Geen resultaten" @allowClear={{true}} as |option select|>
  {{highlight-substr option.searchName select.lastSearchedText}}
</PowerSelect>
