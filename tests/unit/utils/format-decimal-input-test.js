import formatDecimalInput from 'rollvolet-crm/utils/format-decimal-input';
import { module, test } from 'qunit';

module('Unit | Utility | format-decimal-input', function(/*hooks*/) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = formatDecimalInput();
    assert.ok(result);
  });
});
