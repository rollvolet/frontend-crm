<PaperButton
  @onClick={{@onClick}}
  @iconButton={{@iconButton}}
  @raised={{not @iconButton}}
  @primary={{not @iconButton}}>
  {{#if @iconButton}}
    {{paper-icon "restore"}}
    <PaperTooltip>Hergebruik BTW-attest</PaperTooltip>
  {{else}}
    Herbruik attest
  {{/if}}
</PaperButton>
