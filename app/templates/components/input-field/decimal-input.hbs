<PaperInput @label={{@label}} @value={{this.formattedValue}} @errors={{@errors}} @onChange={{fn (mut this.formattedValue)}} @onBlur={{fn this.updateValue this.formattedValue}} @class="default" @required="style" />
