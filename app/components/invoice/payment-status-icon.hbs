{{#let (if @isPaid "text-green-400" "text-red-400") as |color|}}
  {{#let (if @isBooked "file-lock-line" "money-euro-circle-fill") as |icon|}}
    {{#let (if @isBooked "h-5 w-5" "h-4 w-4") as |size|}}
      {{svg-jar icon class=(concat "flex-none fill-current " size " " color)}}
    {{/let}}
  {{/let}}
{{/let}}