{{#if (or @date this.by)}}
  {{#if @resource}}
    {{@resource}} aangemaakt
  {{else}}
    Aangemaakt
  {{/if}}
  {{#if @date}}
    op <Fmt::DateTime @value={{@date}} />
  {{/if}}
  {{#if this.by}}
    door {{this.by}}
  {{/if}}
{{/if}}