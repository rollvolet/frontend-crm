<PaperTabs @selected="main.requests.edit" @borderBottom={{true}} as |tabs|>
  <tabs.tab @value="main.requests.edit" @href={{href-to "main.requests.edit" model}}>
    Aanvraag
  </tabs.tab>
</PaperTabs>

<RequestPanel @model={{model}} @editMode={{editMode}} @onOpenEdit={{action (mut editMode) true}} @onCloseEdit={{action (mut editMode) false}} />

{{liquid-outlet}}
