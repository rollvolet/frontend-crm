import mathSum from 'rollvolet-crm/utils/math/sum';
import { module, test } from 'qunit';

module('Unit | Utility | math/sum', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = mathSum();
    assert.ok(result);
  });
});
