{{#if configuration.preloadStaticLists.isError}}
  {{paper-icon "error"}}
  <PaperTooltip>Fout bij ophalen data</PaperTooltip>
{{else if configuration.preloadStaticLists.isRunning}}
  <PaperProgressCircular @class="md-progress-white" @diameter={{25}} />
{{/if}}
