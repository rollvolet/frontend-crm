<label class="power-select--label">{{this.label}}</label>
<PowerSelect @placeholder={{@label}} @searchPlaceholder="Typ om te zoeken" @options={{this.options}} @selected={{this.selected}} @searchEnabled={{true}} @searchField="name" @onChange={{@onSelectionChange}} @noMatchesMessage="Geen resultaten" @allowClear={{true}} as |option select|>
  {{highlight-substr option.name select.lastSearchedText}}
</PowerSelect>
