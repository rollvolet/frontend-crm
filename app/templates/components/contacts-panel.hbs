{{#liquid-if displayList class="data-table-selection"}}
  {{contacts-table customer=customer onClickRow=(action "openDetail") onEdit=(action "openEdit")}}
  <div class="layout-row layout-align-end">
    {{#paper-button primary=true fab=true raised=true mini=true onClick=(action "openCreate")}}{{paper-icon "add"}}{{/paper-button}}
  </div>
{{else if displayDetail}}
  {{contact-detail-panel model=selectedContact onClose=(action "closeDetail") onEdit=(action "openEdit")}}
{{else if displayCreate}}
  {{#customer-entity-edit-form scope='contact' model=selectedContact onClose=(action "closeEdit") as |f|}}
    <div class="layout-row layout-align-start-center">
      <h3>Nieuw contact</h3>
      {{save-status-icon model=f.model task=f.save}}
    </div>
  {{/customer-entity-edit-form}}
{{else if displayEdit}}
  {{#customer-entity-edit-form scope='contact' model=selectedContact onClose=(action "closeEdit") as |f|}}
    <div class="layout-row layout-align-start-center">
      <h3>Bewerk contact</h3>
      {{save-status-icon model=f.model task=f.save}}
    </div>
  {{/customer-entity-edit-form}}
{{/liquid-if}}